// An Enablock that uses SHA-3 and stores data of users
// Uses for tracking creation of assets by users and users

var mongoose = require('mongoose');
var Schema = mongoose.Schema;
const SHA3 = require('crypto-js/sha3');

var enaBlockSchema = new Schema({
    
})